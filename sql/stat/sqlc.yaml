version: "2"
sql:
  - engine: "sqlite"
    queries: "queries.sql"
    schema: "migrations"
    gen:
      go:
        package: "sqlstat"
        out: "../../sqlgen/sqlstat"
        emit_pointers_for_null_types: true
        overrides:
